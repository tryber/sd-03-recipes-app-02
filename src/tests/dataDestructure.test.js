import {ingredientsList, destructureRecipe, svRecipes} from '../utils/dataDestructure';
import oneMeal from '../../cypress/mocks/oneMeal';

describe('dataDestructure.js tests', () => {
  test('ingredientsList return correct array', () => {

  })
})
